<Page x:Class="_106_Assessment_2.View.Pages.Register"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_106_Assessment_2.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegisterPage">
    <Border Grid.Column="1" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Create an Account" FontSize="22"
                           FontWeight="Bold" Margin="0,0,0,18" />

            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                <TextBlock Text="Username" />
                <TextBox x:Name="txtUsername" Margin="0,4,0,0" Height="34"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                <TextBlock Text="Email" />
                <TextBox x:Name="txtEmail" Margin="0,4,0,0" Height="34"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,0,0,8">
                <TextBlock Text="Password" />
                <PasswordBox x:Name="pwdPassword" Margin="0,4,0,0" Height="34"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Margin="0,0,0,8">
                <TextBlock Text="Confirm Password" />
                <PasswordBox x:Name="pwdConfirm" Margin="0,4,0,0" Height="34"/>
            </StackPanel>

            <StackPanel Grid.Row="5" Margin="0,8,0,0">
                <Button Content="Register" Width="130" Height="34" HorizontalAlignment="Right"
                            Background="#C59E00" Foreground="White" BorderBrush="{x:Null}"
                            Click="Register_Click"/>
                <TextBlock x:Name="lblStatus" Foreground="Crimson" Margin="0,8,0,0"/>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Already have an account?" VerticalAlignment="Center" />

                    <TextBlock Margin="6,0,0,0" VerticalAlignment="Center">
                            <Hyperlink Click="Register_Click">Sign in</Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Page>
