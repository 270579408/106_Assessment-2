<Page x:Class="_106_Assessment_2.View.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="SignIn" Background="#FFF8F8F8">


    <Border Background="White" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Sign in to Onewhero Bay" FontSize="22"
                           FontWeight="Bold" Margin="0,0,0,18" />

            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                <TextBlock Text="Username" />
                <TextBox x:Name="txtUsername" Margin="0,4,0,0" Height="34"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                <TextBlock Text="Password" />
                <PasswordBox x:Name="pwdPassword" Margin="0,4,0,0" Height="34"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Log in" Width="120" Height="34" Click="SignIn_Click"
                            Background="#C59E00" Foreground="White" BorderBrush="{x:Null}" />
            </StackPanel>

            <StackPanel Grid.Row="4" Margin="0,16,0,0">
                <TextBlock>
                    Don't have an account?
                    <Hyperlink Click="_Click">Register</Hyperlink>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Border>
</Page>