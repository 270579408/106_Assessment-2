<Page x:Class="_106_Assessment_2.View.Pages.Community"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Events"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:userControls="clr-namespace:_106_Assessment_2.View.UserControls"
      mc:Ignorable="d"
      d:DesignWidth="1140"
      d:DesignHeight="1024"
      Background="White">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Background="White">

        <StackPanel>

            <!-- Title -->
            <TextBlock Text="Community"
                       FontSize="32"
                       FontWeight="SemiBold"
                       Margin="30 30 30 0"/>

            <Grid Margin="30 30 30 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" 
                    CornerRadius="5" 
                    BorderThickness="1" 
                    BorderBrush="Black">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <TextBlock Text="Type a message..." 
                                   VerticalAlignment="Center"
                                   FontSize="20"
                                   Padding="15 0"
                                   Foreground="#636363"
                                   x:Name="Placeholder_Input"/>

                            <TextBox Height="50" 
                                 Background="Transparent" 
                                 BorderBrush="Transparent"
                                 VerticalContentAlignment="Center"
                                 FontSize="20"
                                 Padding="15 0"
                                 x:Name="MessageInput"
                                 TextChanged="Input_TextChanged"/>
                        </Grid>

                        <Button Grid.Column="1" 
                            Background="Transparent" 
                            BorderBrush="Transparent" 
                            BorderThickness="0" 
                            Padding="0" 
                            Cursor="Hand"
                            Click="SendMessage_Click">
                            <Image Source="/Resources/sent-alt.png" 
                           Height="25" 
                           Width="25" 
                           VerticalAlignment="Center" 
                           Margin="15 0"
                           Grid.Column="1"/>
                        </Button>
                    </Grid>
                </Border>
                <Button Grid.Column="1"
                        Background="Transparent" 
                        BorderBrush="Transparent" 
                        BorderThickness="0"
                        Cursor="Hand"
                        Padding="10"
                        Margin="20 0 0 0"
                        Click="SelectImage_Click">
                    <Image Source="/Resources/add.png" 
                           Height="35" 
                           Width="35" 
                           VerticalAlignment="Center"
                           Grid.Column="1"/>
                </Button>
            </Grid>

        </StackPanel>
    </ScrollViewer>
</Page>